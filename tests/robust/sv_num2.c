
int main() {
    int x;
    int y;
    int z;
    int w;
    while (? && y < 10000) {
    
	if (?) {
	    x = x + 1;
	    y = y + 100;
	} else if (?) {
	    if (x >= 4) {
		x = x + 1;
		y = y + 1;
	    }
	} else if (y > 10*w && z >= 100*x) {
	    y = -y;
	}
	w = w + 1;
	z = z + 10;
    
    }
    //__VERIFIER_assert(x >= 4 && y <= 2);
    return 0;
}